// scrivere una funzione trovaIndice() che, dati un array e un elemento in input, restituisca la posizione in cui si trova l'elemento all'interno dell'array; se l'elemento non è presente nell'array, allora la funzione deve restituire -1

//creare un array e un elemento di input che si trova anche nell'array
//creare una funzione che si occupi di individurare l'elemento di input all'interno dell'array e di idnciare il suo indice se l'elemento non verrà trovato la funzione dpvrà stampare -1



const myArray = ["paperella", "minnie", "naruto", "goku"];

const inputElem = "minnie";

const findElem = foundIndex (myArray, inputElem);
console.log (findElem);

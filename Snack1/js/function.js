/**
 * Description: la funzione avrà il compito di contare gli elementi presenti all'interno di un array
 * @param {array} array
 * @returns {number}
 */
function elemCount (array){   

    const arrayNum = array.length;

    return arrayNum;
}